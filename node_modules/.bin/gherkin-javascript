#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gherkin/bin/gherkin" "$@"
  ret=$?
else 
  node  "$basedir/../gherkin/bin/gherkin" "$@"
  ret=$?
fi
exit $ret
