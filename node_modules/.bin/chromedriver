#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../electron-chromedriver/chromedriver.js" "$@"
  ret=$?
else 
  node  "$basedir/../electron-chromedriver/chromedriver.js" "$@"
  ret=$?
fi
exit $ret
